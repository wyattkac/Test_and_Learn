"""Models
"""

#   Make new model (main/models.py), add to installed apps (mysite/settings.py)
# cd mysite
# python manage.py makemigrations
# python manage.py migrate
# python manage.py shell    #To interact with website through the shell
    # from main.models import Tutorial
    # from django.utils import timezone
    # new_tutorial = Tutorial(tutorial_title="To be", tutorial_content="or not to be. That is the question.", tutorial_published=timezone.now())
    # new_tutorial.save()
    # Tutorial.objects.all()
